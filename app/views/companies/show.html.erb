<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-3">All Jobs for <%= @company.name %></h1>
  </div>
</div>

<% @company.jobs.each do |job| %>
  <ul><%= job.title %></ul>
<% end %>
